# Etapa 1 — A06-E0 a A06-E3

Abra `app.py` e `data/entrada.json`. Primeiro responda E0–E3 sem executar. Depois abra o terminal nesta pasta e execute `python app.py` no Windows ou `python3 app.py` no macOS/Linux.
