# Etapa 2 — A06-E4

Abra `validacao.py` e substitua somente `LACUNA_1` a `LACUNA_4`. Depois execute `python executar_casos.py` ou `python3 executar_casos.py`. Registre as três saídas em `../RESPOSTAS.md`.
